const OWNER='shandianchengzi',REPO='cook-videos',BRANCH='main',MANUAL='manual/overrides.json';
const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),fmt=n=>new Intl.NumberFormat('zh-CN',{notation:'compact'}).format(n||0);
let videos=[],dishes={},ingredients={},manual={},editing=null;
async function getJson(path){const r=await fetch(path+'?v='+Date.now());if(!r.ok)throw Error(`${path}: ${r.status}`);return r.json()}
async function boot(){[videos,dishes,ingredients,manual]=await Promise.all([getJson('村驴/videos.json').then(x=>x.videos||[]),getJson('base/dishes.json'),getJson('base/ingredients.json'),getJson(MANUAL)]);$('dish').innerHTML+=[...new Set(videos.flatMap(v=>v['菜名']||[]))].sort().map(x=>`<option>${esc(x)}</option>`).join('');$('ingredient').innerHTML+=Object.keys(ingredients).sort().map(x=>`<option>${esc(x)}</option>`).join('');$('token').value=localStorage.getItem('cookVideosGithubToken')||'';$('remember').checked=!!$('token').value;render()}
function videoIngredients(v){return [...new Set((v['菜名']||[]).flatMap(n=>(dishes[n]?.ingredients||[])))]}
function render(){const term=$('q').value.trim().toLowerCase(),d=$('dish').value,ing=$('ingredient').value;const rows=videos.filter(v=>(!d||(v['菜名']||[]).includes(d))&&(!ing||videoIngredients(v).includes(ing))&&(!term||`${v.title}\n${v.description}\n${(v['菜名']||[]).join(' ')}\n${videoIngredients(v).join(' ')}`.toLowerCase().includes(term)));$('count').textContent=`${rows.length} 个视频`;$('grid').innerHTML=rows.map(v=>`<article><a href="https://www.bilibili.com/video/${encodeURIComponent(v.bvid)}"><img loading="lazy" src="${esc(v.cover_url)}@672w_378h_1c.webp" alt=""></a><div class="body"><div class="tags">${(v['菜名']||[]).map(x=>`<span>${esc(x)}</span>`).join('')||'<span class="muted">待识别</span>'}</div><h2>${esc(v.title)}</h2><p>${esc(v.description||'暂无简介')}</p><small>${esc(v.bvid)} · ${fmt(v.stat?.view)} 播放 · ${Math.floor((v.duration||0)/60)}:${String((v.duration||0)%60).padStart(2,'0')}</small><button class="edit" data-bvid="${esc(v.bvid)}">编辑菜名与食材</button></div></article>`).join('')||'<p class="empty">暂无匹配视频。</p>';document.querySelectorAll('.edit').forEach(b=>b.onclick=()=>openEditor(b.dataset.bvid))}
function openEditor(bvid){editing=videos.find(v=>v.bvid===bvid);const names=manual.video_dishes?.[bvid]??editing['菜名']??[];$('editVideo').textContent=`${editing.title}（${bvid}）`;$('editDishes').value=names.join('\n');$('editIngredients').value=[...new Set(names.flatMap(n=>(manual.dishes?.[n]?.ingredients??dishes[n]?.ingredients??[])))].join('\n');$('status').textContent='';$('editor').showModal()}
function lines(id){return [...new Set($(id).value.split(/\r?\n|[,，]/).map(x=>x.trim()).filter(Boolean))]}
function buildManual(){const names=lines('editDishes'),ings=lines('editIngredients'),next=structuredClone(manual);next.video_dishes??={};next.dishes??={};next.ingredients??={};next.video_dishes[editing.bvid]=names;for(const i of ings)next.ingredients[i]={...(next.ingredients[i]||{}),name:i,processing_methods:next.ingredients[i]?.processing_methods||[],notes:next.ingredients[i]?.notes||[],sources:next.ingredients[i]?.sources||[`manual:${editing.bvid}`]};for(const n of names)next.dishes[n]={...(next.dishes[n]||{}),name:n,ingredients:ings,special_tools:next.dishes[n]?.special_tools||[],sources:next.dishes[n]?.sources||[`manual:${editing.bvid}`]};return next}
function downloadManual(){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(buildManual(),null,2)+'\n'],{type:'application/json'}));a.download='overrides.json';a.click();URL.revokeObjectURL(a.href)}
function b64utf8(s){const bytes=new TextEncoder().encode(s);let bin='';bytes.forEach(b=>bin+=String.fromCharCode(b));return btoa(bin)}
async function submitManual(){const token=$('token').value.trim();if(!token){$('status').textContent='请填写 Fine-grained Token，或下载 JSON 后手动提交。';return}if($('remember').checked)localStorage.setItem('cookVideosGithubToken',token);else localStorage.removeItem('cookVideosGithubToken');$('submit').disabled=true;$('status').textContent='正在读取并提交…';try{const h={Accept:'application/vnd.github+json',Authorization:`Bearer ${token}`,'X-GitHub-Api-Version':'2022-11-28'},url=`https://api.github.com/repos/${OWNER}/${REPO}/contents/${MANUAL}?ref=${BRANCH}`,current=await fetch(url,{headers:h});if(!current.ok)throw Error(`读取失败 ${current.status}`);const meta=await current.json(),latest=JSON.parse(new TextDecoder().decode(Uint8Array.from(atob(meta.content.replace(/\n/g,'')),c=>c.charCodeAt(0)))),mine=buildManual();manual={...latest,...mine,video_dishes:{...(latest.video_dishes||{}),...(mine.video_dishes||{})},dishes:{...(latest.dishes||{}),...(mine.dishes||{})},ingredients:{...(latest.ingredients||{}),...(mine.ingredients||{})}};const body={message:`manual: revise ${editing.bvid}`,content:b64utf8(JSON.stringify(manual,null,2)+'\n'),sha:meta.sha,branch:BRANCH},put=await fetch(`https://api.github.com/repos/${OWNER}/${REPO}/contents/${MANUAL}`,{method:'PUT',headers:{...h,'Content-Type':'application/json'},body:JSON.stringify(body)});if(!put.ok)throw Error(`提交失败 ${put.status}: ${await put.text()}`);$('status').textContent='提交成功。Action 将重新预处理数据并部署页面。'}catch(e){$('status').textContent=e.message}finally{$('submit').disabled=false}}
['q','dish','ingredient'].forEach(id=>$(id).addEventListener(id==='q'?'input':'change',render));$('download').onclick=downloadManual;$('submit').onclick=submitManual;boot().catch(e=>{$('grid').textContent='数据加载失败：'+e.message});
