const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmt=n=>new Intl.NumberFormat('zh-CN',{notation:'compact'}).format(n||0);
let videos=[];
async function boot(){const r=await fetch('村驴/videos.json');const j=await r.json();videos=j.videos||[];const ds=[...new Set(videos.flatMap(v=>v['菜名']||[]))].sort();dish.innerHTML+=[...ds].map(x=>`<option>${esc(x)}</option>`).join('');render()}
function render(){const term=q.value.trim().toLowerCase(), d=dish.value;const rows=videos.filter(v=>(!d||(v['菜名']||[]).includes(d))&&(!term||`${v.title}\n${v.description}\n${(v['菜名']||[]).join(' ')}`.toLowerCase().includes(term)));count.textContent=`${rows.length} 个视频`;grid.innerHTML=rows.map(v=>`<article><a href="https://www.bilibili.com/video/${encodeURIComponent(v.bvid)}"><img loading="lazy" src="${esc(v.cover_url)}@672w_378h_1c.webp" alt=""></a><div class="body"><div class="tags">${(v['菜名']||[]).map(x=>`<span>${esc(x)}</span>`).join('')||'<span class="muted">待识别</span>'}</div><h2>${esc(v.title)}</h2><p>${esc(v.description||'暂无简介')}</p><small>${esc(v.bvid)} · ${fmt(v.stat?.view)} 播放 · ${Math.floor((v.duration||0)/60)}:${String((v.duration||0)%60).padStart(2,'0')}</small></div></article>`).join('')||'<p class="empty">暂无匹配视频；首次 Action 完成后会显示数据。</p>'}
q.addEventListener('input',render);dish.addEventListener('change',render);boot().catch(e=>{grid.textContent='数据加载失败：'+e.message});
